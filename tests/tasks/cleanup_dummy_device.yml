---
- name: Clean up dummy testing device
  file:
    path: "{{ nbde_client_test_device }}"
    state: absent

- name: Clean up dummy keyfile
  when: nbde_client_test_keyfile is defined
  file:
    path: "{{ nbde_client_test_keyfile }}"
    state: absent

# vim:set ts=2 sw=2 et:
