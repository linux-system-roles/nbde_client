---
- name: Verify the default keyfile works
  shell: >
    cryptsetup open --test-passphrase "{{ nbde_client_test_device }}"
    --key-file "{{ nbde_client_test_keyfile }}"
  ignore_errors: yes
  register: nbde_client_keyfile

# vim:set ts=2 sw=2 et:
